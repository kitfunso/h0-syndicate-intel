# dependencies
/node_modules
/.pnp
.pnp.*

# next
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (NEVER commit credentials)
.env
.env*.local
.env.local

# google service account keys
*.sa.json
service-account*.json
gcp-key*.json

# real seed data (extracted via Synth) stays out of git; only the .example.json is tracked
/data/spike-seed.json
/data/dataset/

# vercel
.vercel

# typescript
*.tsbuildinfo
